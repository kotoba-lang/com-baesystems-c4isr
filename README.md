# Baesystems_c4isr Clean Room Actor

Clean-room API-compatible implementation of the baesystems_c4isr vertical monopoly, backed by Datomic and Py Kotodama WASM.

## Provenance

Relocated 2026-07-04 from `etzhayyim/root/20-actors/baesystems_c4isr-compat` to
`kotoba-lang/com-baesystems-c4isr` per the org-taxonomy library-placement rule (any
library/substrate code belongs in `kotoba-lang`, ADR-2606302300), following
the same relocation pattern as `kami-nv-compat` (ADR-2607020130). See
ADR-2607041500 for the full ~1,027-repo migration plan and naming convention.
